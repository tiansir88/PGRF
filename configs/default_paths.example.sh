#!/usr/bin/env bash

# Root directory for project-level generated files.
export PGRF_PROJECT_DIR=/path/to/pgrf_project

# Working manifest/cache directory used by the scripts.
export PGRF_BASE_DIR=/path/to/pgrf_project/training_manifest

# MIMIC-IV-ECG waveform root.
export PGRF_MIMIC_ECG_ROOT=/path/to/mimic-iv-ecg

# Optional run name override for selected training/evaluation scripts.
# export RUN_NAME=pgrf_train_v1
